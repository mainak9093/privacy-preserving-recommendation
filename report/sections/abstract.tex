%% ==========================================================================
%%  ABSTRACT --- written LAST, once the survey knows what it argues.
%% ==========================================================================
\begin{abstract}
\todo{Write this last. Roughly 200 words, four moves:
      (1) recommender systems leak consumption history, and the data is both
      valuable and dangerous;
      (2) two literatures have grown up separately --- private \emph{training}
      of the model, and private \emph{retrieval} of the recommended item;
      (3) \pirsonaplain{} is close to the only system spanning both and now
      predates the whole modern private-retrieval line, while \nudgeplain{}
      is the state of the art on training and explicitly delegates the fetch
      to ``other means'';
      (4) this survey maps both literatures, identifies the gap between them,
      and states what we propose to build.
      Do not oversell. Name the two base papers explicitly --- the reader is
      the author of one of them.}
\end{abstract}
