# ─────────────────────────────────────────────────────────────
#  PRIVATE — local only, never pushed to the team repo
#  (see RULES.md B1/B2)
# ─────────────────────────────────────────────────────────────
RULES.md
MEMORY.md
CLAUDE.md
AGENTS.md
.claude/
.local/
project_survey/
graphify-out/

# ─────────────────────────────────────────────────────────────
#  Data — downloaded by scripts/fetch_data.py, never committed
# ─────────────────────────────────────────────────────────────
# ANCHORED with a leading slash, deliberately. An unanchored "data/" matches a
# directory of that name at ANY depth, which silently swallowed
# tests/data/fixedpoint_vectors.txt, the committed cross-language contract
# vectors. That file has to be in the repo or the boundary test cannot run on a
# fresh clone. Only the top-level MovieLens download is meant to be ignored.
/data/
model/out/
*.bin
!tests/**/*.bin
!tests/data/**

# ─────────────────────────────────────────────────────────────
#  Generated artefacts
#  Raw benchmark data (bench/results/*.jsonl) IS committed.
#  Figures are generated by `make figures` and are NOT.
# ─────────────────────────────────────────────────────────────
bench/figures/

# Raw wire CAPTURES are not committed, though the benchmark ROWS beside them
# are. distinguisher.jsonl is 1.5 MB of base64 and transcript.jsonl 270 KB, and
# both are regenerated by `mingw32-make distinguisher` / `demo-net`. What a
# reader can actually check -- the analysis and its confidence intervals -- is
# in distinguisher_result.jsonl, which IS committed, alongside the script that
# produced it. Same reasoning as figures: keep what is checkable, regenerate
# what is bulk.
bench/results/distinguisher.jsonl
bench/results/transcript.jsonl
*.pdf
!docs/**/*.pdf
# The two instructor-recommended papers are open access and shared with the team,
# so everyone reads the same version.
!references/*.pdf
# The built survey PDF ships alongside its sources.
!report/survey.pdf
# The report PDFs, so teammates can read them without a LaTeX toolchain.
# midterm.pdf was held back while the only build on disk was the stale
# 2026-08-15 stub, since tracking that would have shipped a misleading
# artefact. The real two-page report landed 2026-09-11, so the exception this
# comment promised is now added. It is the GRADED deliverable; it belongs in
# the repo.
!report/midterm_technical.pdf
!report/midterm.pdf
# The presentation deck, built from report/slides/survey_slides.tex, which IS
# tracked -- the deck is reproducible from source (needs the metropolis theme).
!report/slides/survey_slides.pdf

# ─────────────────────────────────────────────────────────────
#  Build
# ─────────────────────────────────────────────────────────────
build/
cmake-build-*/
CMakeFiles/
CMakeCache.txt
compile_commands.json
*.o
*.a
*.so
*.dll
*.exe
*.obj
*.lib

# ─────────────────────────────────────────────────────────────
#  Python
# ─────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ─────────────────────────────────────────────────────────────
#  LaTeX (report sources under docs/)
# ─────────────────────────────────────────────────────────────
*.aux
*.log
*.out
*.toc
*.bbl
*.blg
*.synctex.gz
*.fls
*.fdb_latexmk
# Beamer navigation aux
*.nav
*.snm
*.vrb

# ─────────────────────────────────────────────────────────────
#  Editors / OS
# ─────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini

# ─────────────────────────────────────────────────────────────
#  C++ build output (Makefile writes here)
# ─────────────────────────────────────────────────────────────
build/
